object answers{
    def main(args: Array[String])={

        //question 1 and 2 answers
        //println("Question 1 and 2")
        var k = 2; var i = 2; var j = 2;
        var m = 5; var n = 5;
        var f = 12.0; 
        var g = 4.0;
        var c = 'X';
        
        //question 3 answers
        println("Question 3")
        println(k + 12 * m)
        println(m / j)
        println(n % j)
        println(m / j * j)
        println(f + 10*5 +g)
       // println(++i * n)   scala doesn't support increment and decrement operators    	
       
    
    }
}
